// Production environment. In a real deployment the apiBaseUrl would be the
// absolute URL of the production API.
export const environment = {
  production: true,
  apiBaseUrl: '/api',
  enableNgrxDevtools: false
};
